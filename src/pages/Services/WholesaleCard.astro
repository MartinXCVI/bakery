---
interface Props {
  src: string;
  title: string;
  text: string;
}

const { src, title, text } = Astro.props
---

<div class="wholesale-card">
  <figure class="wholesale-fig">
    <img src={src} alt={title}>
  </figure>
  <h3 class="wholesale-product-name">{title}</h3>
  <p class="wholesale-product-text">{text}</p>
</div>